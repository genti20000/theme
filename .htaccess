RewriteEngine On

# Serve existing files/directories normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback all app routes to index.html for SPA routing
RewriteRule ^ index.html [L]
